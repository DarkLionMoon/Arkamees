# ════════════════════════════════════
# ARCAMIS — Cloudflare Pages Config
# ════════════════════════════════════

name = "arkamees"
compatibility_date = "2024-01-01"

# Fondamentale per Cloudflare Pages:
# Indica la cartella dove si trovano i tuoi file statici (index.html, css, etc.)
# Se non hai una build, metti "." per la root o "public" se li hai lì.
pages_build_output_dir = "." 

[vars]
# NOTION_TOKEN va caricato tramite la Dashboard di Cloudflare Pages 
# sotto Settings -> Functions -> Variables
